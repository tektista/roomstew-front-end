import { StyleSheet, Text, View, ScrollView } from "react-native";
import React from "react";

export default function NumberInputScreen({ navigation }) {
  return (
    <ScrollView>
      <Text>NumberInputScreen</Text>
    </ScrollView>
  );
}

const styles = StyleSheet.create({});
